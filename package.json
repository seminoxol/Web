{
  "name": "pci-glass-website",
  "version": "1.0.0",
  "description": "PCI Glass — official website with quote request backend",
  "main": "server.js",
  "scripts": {
    "build": "node scripts/build-style.js",
    "prestart": "npm run build",
    "start": "node server.js",
    "dev": "nodemon server.js"
  },
  "dependencies": {
    "compression": "^1.7.4",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "express-rate-limit": "^7.5.0",
    "helmet": "^8.0.0",
    "nodemailer": "^9.0.1"
  },
  "devDependencies": {
    "nodemon": "^3.1.0"
  }
}
